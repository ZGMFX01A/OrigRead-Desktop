# 原读 Desktop 操作手册

[返回项目介绍](https://github.com/ZGMFX01A/OrigRead-Desktop/blob/main/README-zh-CN.md) · [English](https://github.com/ZGMFX01A/OrigRead-Desktop/blob/main/USER_GUIDE.md)

本手册适用于 Windows、macOS 和 Linux 桌面端。按你现在想做的事，跳到对应章节即可。

## 快速索引

- [快速开始](#快速开始)
- [添加一个来源](#添加一个来源)
- [管理 RSSHub](#管理-rsshub)
- [阅读文章](#阅读文章)
- [配置 AI](#配置-ai)
- [使用 AI 摘要](#使用-ai-摘要)
- [使用 Reader AI Chat](#使用-reader-ai-chat)
- [Citation：核对回答的依据](#citation核对回答的依据)
- [搜索与工具](#搜索与工具)
- [翻译文章](#翻译文章)
- [把文章分享为 Markdown](#把文章分享为-markdown)
- [朗读文章](#朗读文章)
- [键盘快捷键](#键盘快捷键)
- [添加来源失败怎么办](#添加来源失败怎么办)
- [使用账户与同步](#使用账户与同步)
- [规则与过滤](#规则与过滤)
- [OPML、备份与迁移](#opml备份与迁移)
- [安装与更新](#安装与更新)
- [常见问题](#常见问题)

---

## 快速开始

先保留默认 **Local** 账户，添加一个你想读的来源，再打开一篇文章试试全文阅读。需要摘要或问文章时配置 AI，需要阅读外语时配置翻译，其他设置可以以后再调整。

这份手册也能在应用的 **设置 → 关于与支持 → 用户操作手册** 中打开。

---

## 添加一个来源

订阅不一定从 RSS 地址开始。网站首页、栏目页、Feed 或公开 API 地址，都可以交给原读检测。

1. 打开左侧的**添加订阅**菜单，进入添加来源。
2. 粘贴地址并开始检测，等待结果出现。窗口会显示当前阶段和等待时间。
3. 查看候选的栏目、文章数量和状态，选择真正想追踪的内容。
4. 确认添加，回到时间线等待首次同步。

想追踪某个专栏时，优先使用专栏的列表页。还没想好订阅什么，可以浏览内置来源目录；从其他阅读器迁移，则先导出 OPML，再从添加菜单选择**导入 OPML**。

### 怎么选解析结果

| 来源方式 | 可以订阅什么 | 选择时看什么 |
| --- | --- | --- |
| RSS / Atom | 网站提供的标准 Feed，或原读从网页中发现的 Feed | 更新正常、内容符合预期时，通常可以直接选 |
| RSSHub | 已有路由支持的网站和频道 | 是否是目标栏目，以及当前实例是否可用 |
| 网页解析（Website） | 页面上有规律排列的文章列表 | 标题、链接是否正确，有没有混入导航或广告 |
| JSON / API | 公开接口、WordPress 文章数据，以及部分 Next.js / Nuxt 页面内的数据 | 是否找到了需要的文章记录 |
| 动态网页 | 需要加载脚本后才出现的文章列表 | 渲染后的文章数量和可信度提示 |

原读会检查标题、链接、时间和文章数量等信息，再推荐较合适的候选。同一网址可能有几个栏目或解析方式，推荐结果可以作为起点，最终以你需要的内容为准。

网页、JSON/API 和 RSSHub 等扩展来源需要使用 **Local** 账户。远端账户的能力见[账户与同步](#使用账户与同步)。

---

## 管理 RSSHub

RSSHub 路由描述“怎样获取这个网站的文章”，实例是实际返回内容的服务。找到路由后，仍需要可用实例才能订阅。

打开 **设置 → RSSHub**，启用 RSSHub，添加或启用一个可访问的实例，再测试连接。可以配置多个公共或自建实例，不需要在电脑上运行 RSSHub 服务器才能使用客户端。

如果显示“已匹配”却没有可订阅项，先看是实例超时、返回内容无效，还是缺少更具体的网址。测试或切换实例后再试；一次请求失败不需要删除路由数据。

---

## 阅读文章

| 内容 | 适合什么时候看 |
| --- | --- |
| **来源正文** | Feed 或接口直接提供的内容，可能是全文，也可能只有摘要 |
| **全文** | 原读从文章网页提取的正文，适合连续阅读、翻译和 AI 分析 |
| **原文** | 网站真实页面，在应用内网页视图中打开，适合评论、图表或互动内容 |

来源只有几行摘要时，先尝试全文；提取不完整时，打开原文查看。首次获取全文可能需要等待。

阅读页可以标记已读、收藏、切换文章和搜索正文。在阅读外观设置中，可以调整字体、字号、行距、背景和版心，也能导入本地字体。

文章与 AI 面板可以并排显示。使用面板上的位置选项停靠到左侧或右侧，拖动它与正文之间的边缘调整宽度；只想看文章时，可以进入专注阅读。常用操作见[键盘快捷键](#键盘快捷键)。

---

## 配置 AI

想生成摘要或围绕文章提问，先连接一个 OpenAI 兼容服务：

1. 打开 **设置 → AI 阅读 → 模型服务**，添加服务地址。
2. 按服务要求填写 API Key，获取模型列表或手动填写模型名称。
3. 选择默认模型，测试连接，再保存并启用服务。
4. 在**阅读**页启用 AI 阅读，确认默认服务、模型、输出语言和摘要档位。

地址、密钥和模型名称以服务提供方的信息为准。可以配置多个服务，在摘要选项或对话输入区临时切换。

### 设置放在哪里

| 页面 | 用来设置什么 |
| --- | --- |
| **阅读** | AI 开关、默认服务与模型、输出语言、摘要档位 |
| **模型服务** | 服务地址、密钥、模型与连接测试 |
| **网络搜索** | 搜索模式、搜索服务和结果数量 |
| **回答与快捷操作** | 自定义指令、Skills、快捷消息、远程和本地 MCP |

密钥默认隐藏，需要检查时再点击显示。保存或离开后，设置页会重新隐藏敏感内容。

---

## 使用 AI 摘要

打开文章，选择 **AI 摘要**，按需要使用**速览 / 均衡 / 深入**。生成时会显示阶段和等待时间，长文章或响应较慢的模型可能需要多等一会儿；不想继续时可以停止。

成功的摘要会保存，文章正文未变化时可以再次查看。想换模型或详细程度，在摘要选项里重新选择后生成即可，这次选择不会改变全局默认值。

---

## 使用 Reader AI Chat

打开一篇文章，展开 AI 对话面板，在输入区选择服务和模型，再发送问题。例如“作者的主要论据是什么？”“这段话和前面的判断一致吗？”。

只想问某句话时，在正文中选中文字，再使用 **Ask AI**。选中的内容只附带到下一次请求，不会反复加入之后每一条消息。

### 比较几篇文章

点击输入区附近的**回形针**，从最近文章或标题搜索结果中选择附件。最多可额外加入 **5 篇**，当前文章不占这个额度。选好后可以问“这些文章有哪些共同观点，哪里存在分歧？”。

只有选中的文章才会作为附件提供给模型。回答的来源信息可以查看当时使用的内容；之后增删附件，不会改写旧回答的依据。

### 继续上次的讨论

会话按主文章保存，可以新建、切换、重命名、删除，也可以搜索当前对话。生成中点击停止，会保留已经显示的内容；重新生成旧回答时，使用的是原请求当时的文章、选段和附件。

模型提供推理内容时，可以在回答中查看。文章分析适合系统梳理主张、论据和局限，普通问答则适合带着具体问题继续阅读。

---

## Citation：核对回答的依据

读到 AI 的结论时，可以沿着引用查看原话，再判断它是否支持这个结论。

1. 点击回答中的文章引用标记。
2. 原读会定位并高亮对应正文；引用另一篇附加文章时，会打开那篇文章并保留当前讨论。
3. 阅读前后文，再回到旁边的 AI 面板继续查看回答或追问。

比如，比较两篇报道对事件原因的解释时，可以逐一打开各自引用，看看分歧来自哪些原话。文章和回答并排显示，核对时不用反复复制文字或寻找网页。

### 旧回答还能查到依据吗

历史回答保留生成时的来源信息，之后改变附件不会替换这些依据。正文里显示的引用编号也会跟随正在查看的回答，因此不同回答中的相同编号不一定代表同一段内容。

如果文章被改写、删除，或无法准确定位，查看来源面板提供的信息即可。搜索引用会打开网页出处；工具结果有网址时可打开网址，否则在来源信息里查看。引用方便核对，并不意味着 AI 的理解一定正确。

桌面端正文中的引用按钮用于再次定位证据，核对后在旁边的 AI 面板继续阅读即可。

---

## 搜索与工具

### 查背景或近期进展

在 **设置 → AI 阅读 → 网络搜索**中添加搜索服务，填写所需地址或密钥并测试。可以保持 **AUTO**，让需要近期信息的问题使用搜索；设为 **OFF**则关闭自动搜索。输入区的搜索按钮可以只强制下一条消息联网，发送后恢复原模式。

回答会显示搜索过程和结果，可以查看搜了什么、参考了哪些网页。

### 保存常用的提问方法

**快捷消息**保存“列出关键论据”这类常问的问题；**自定义指令**保存“用中文回答，先解释术语”这类长期偏好。**Skills**可以保存更完整的方法和参考资料，并用于摘要、翻译、对话或文章分析。

在 **回答与快捷操作**中管理这些内容。导入 Skill 后，模型使用其中的说明和资料，原读不会执行包内脚本。

### 连接外部工具

需要外部工具时，可以配置远程 MCP 服务，或本机通过命令启动的 MCP 服务。按服务说明填写地址或命令、参数和认证信息，先测试连接并刷新工具。

**每次 MCP 工具执行都需要明确批准**。本地服务在测试、刷新工具或执行时按需启动。普通阅读、摘要和文章问答无需配置 MCP。

具体配置方法见 [AI、Web Search、Skill 与 MCP 指南](https://github.com/ZGMFX01A/OrigRead-Desktop/blob/main/AI_MCP_SKILLS-zh-CN.md)。

---

## 翻译文章

1. 打开 **设置 → 翻译设置**，启用一种服务，填写所需地址或密钥。
2. 选择默认目标语言和服务，测试连接。
3. 回到文章，使用翻译入口开始翻译，再按需要查看译文或双语内容。

可选 Microsoft Translator、DeepL、Google Cloud Translation、DeepLX / DLX 兼容服务，也可以使用已配置的 OpenAI 兼容模型。普通翻译服务无需先配置 AI。

长文章会分段处理，等待时间取决于篇幅和服务速度。难懂的表达可以对照原文查看；DeepL 的翻译测试和额度查询是两个独立操作，额度查询失败不一定代表翻译不可用。

---

## 把文章分享为 Markdown

在阅读页点击**分享**，原读会把内容复制到剪贴板，之后直接粘贴到笔记软件即可。

首次使用时，选择只复制标题和链接，或自定义加入正文、当前已打开的译文和摘要。原文 URL 始终保留；以前生成过但当前没有打开的译文和摘要不会附带发送。

之后点击分享沿用上次选择，**右键分享按钮**可以修改。Markdown 会保留标题、引用、列表、链接和图片外链；图片不会作为文件一起复制，显示效果取决于目标应用的支持情况。

---

## 朗读文章

正文、译文和摘要都有相应的朗读入口。正在显示译文时，主朗读入口优先读译文；摘要面板可以单独朗读摘要。

使用朗读控件开始或停止，按需要选择可用声音。声音列表取决于当前系统提供的语音。

---

## 键盘快捷键

Windows / Linux 使用 Ctrl，macOS 使用 Cmd。

| 快捷键 | 功能 |
| --- | --- |
| `↑` / `↓` | 上下滚动正文 |
| `←` / `→`，或 `K` / `J` | 上一篇 / 下一篇 |
| `M` | 已读 / 未读 |
| `S` | 收藏 / 取消收藏 |
| `U` | 打开 / 关闭原文 |
| `A` | 切换 AI 助手 |
| `[` | 切换专注阅读 |
| `Ctrl/Cmd + F` | 搜索正文；AI 对话打开时优先搜索对话 |
| `Ctrl/Cmd + Shift + F` | 打开全局文章搜索 |
| `Ctrl/Cmd + K` | 聚焦当前列表或来源切换器的搜索框 |
| `Ctrl/Cmd + Shift + K` | 双栏布局中打开来源切换器，需显示工作区 |
| `,` / `.` | 切换已打开的 AI 面板位置 |
| `-` / `+` | 调整已打开的 AI 面板尺寸 |

输入文字、操作弹窗或设置时，阅读快捷键会按当前界面让位给输入操作。

---

## 添加来源失败怎么办

### 检测较慢

先看当前阶段，等待这一轮结束。公共 RSSHub、网页解析和动态渲染通常比直接读取 Feed 慢。首页结果不对时，换成具体栏目列表页再试。

### 动态网页结果不可靠

动态网页需要加载并执行脚本，再寻找文章。某些候选在没有得到可靠列表时，仍可在提示低可信后由你手动选择尝试；它们不会被当作正常结果默认推荐。

**页面能打开不代表可以稳定订阅。** 添加前查看文章数量、标题和链接，添加后观察刷新。已有稳定 Feed、RSSHub 路由或公开接口时，优先使用它们。

### 浏览器能打开，原读却读不到

浏览器可能已有登录状态，网页也可能需要验证码、付费权限或复杂操作。原读不会绕过这些访问条件。可以寻找网站提供的 Feed、可用 RSSHub 或公开接口，必要时通过原文页面阅读。

仍无法订阅时，请[提交 Issue](https://github.com/ZGMFX01A/OrigRead-Desktop/issues)，附上网址、版本、预期栏目和错误提示。

---

## 使用账户与同步

| 账户 | 适合的使用方式 |
| --- | --- |
| **Local** | 数据保存在本机，可使用 RSS、RSSHub、网页解析和 JSON/API |
| **FreshRSS / Google Reader Compatible** | 连接已有服务，同步订阅、分组、文章及已读和收藏状态 |
| **Fever Compatible** | 使用服务通过 Fever 协议提供的 Feed、文章和阅读状态 |

有自己的服务时，在 **设置 → 账户**添加对应账户并同步。同步间隔、启动同步等设置属于当前账户，可以分别调整。

远端账户能维护哪些订阅、同步哪些状态，由服务协议决定。Fever 没有完整的订阅管理能力；网页、JSON/API 和 RSSHub 等原读扩展来源请使用 Local。

---

## 规则与过滤

### 什么时候需要解析规则

自动发现已经找到所需内容时，无需另写规则。需要指定栏目或修正解析结果时，才使用**网站解析规则（Website Rule）**或 **JSON/API 规则**。前者寻找网页文章列表，后者读取公开接口或网页内的文章数据。

在对应设置页可以管理、导入、导出和测试规则；手动编辑时，先查看页面内的规则说明。

### 请 AI 帮忙生成

1. 打开 **设置 → 网站解析规则**或 **JSON 规则**，点击 **AI 生成**。
2. 输入文章列表页、公开 JSON 接口，或含有可识别文章数据的页面地址。
3. 选择 AI 服务和模型，等待生成及试跑。
4. 查看文章数量、评分、样例标题和链接，确认正确后再点**保存规则**。

桌面端 AI 规则生成先验证文章列表。能找到文章，不代表一定能提取完整正文；打开文章时仍可使用常规全文提取和原文查看。网页改版后，重新测试并调整规则。

### 过滤不想看的标题

在 **设置 → 文章过滤**中添加关键词或正则规则。关键词尽量具体，避免屏蔽过多内容；规则可以启停、导入和导出。

过滤只影响之后获取的新文章，**不会删除已经保存的历史文章**。效果不合适时，先停用或修改，再观察后续更新。

---

## OPML、备份与迁移

### 选择迁移方式

| 想做什么 | 使用什么 |
| --- | --- |
| 与其他阅读器交换标准订阅 | OPML |
| 带走订阅、分组、规则、RSSHub、阅读偏好和 AI / 翻译设置 | 完整配置备份 |
| 在多台设备间同步远端账户支持的阅读数据 | 相同服务的账户同步 |

配置备份还可包含搜索、Skills、快捷消息和 MCP 等相关设置。**文章正文、已读和收藏历史、摘要及翻译缓存不在配置备份中。**

### 导出配置

在 **设置 → 备份与恢复**选择**导出完整配置**并保存文件。默认不包含敏感凭据；需要带走已保存的密钥时，先开启**包含 API Key**，设置至少 6 个字符的备份密码，再导出。

### 恢复配置

在目标设备打开 **备份与恢复**，选择**恢复配置**并打开文件。恢复加密备份时，先开启**包含 API Key**以显示密码框，填入原备份密码，再选择恢复文件。

相同网址的订阅会合并，缺少的会新增，其他已有订阅和文章历史保留。恢复完成后检查来源、服务和阅读设置。

### 在 Android 与桌面端之间迁移

两端可通过兼容的配置备份迁移共同支持的订阅、规则和设置。它不是自动同步整个阅读库，也不代表每项平台设置都完全相同。恢复后重新测试服务；本地模型地址、字体文件或本机 MCP 命令，在另一台设备上可能需要重新配置。

---

## 安装与更新

从 [GitHub Releases](https://github.com/ZGMFX01A/OrigRead-Desktop/releases/latest) 下载与你的系统匹配的文件。

| 系统 | 安装方法 |
| --- | --- |
| Windows 11 x64 | 运行 `.exe` 安装程序，按向导选择安装位置 |
| macOS 13+ Apple Silicon | 打开 `.dmg`，将 OrigRead 拖入“应用程序” |
| Linux x64 | 使用 `.AppImage`，或在 Ubuntu / Debian 安装 `.deb` |

AppImage 如果不能直接运行，先在文件属性中允许作为程序执行。

### macOS 提示无法打开

确认应用来自项目官方 Release，且已放在 `/Applications/OrigRead.app`。若系统因下载隔离属性提示“已损坏”或拒绝打开，可以在终端对这一个应用执行：

```bash
sudo xattr -r -d com.apple.quarantine /Applications/OrigRead.app
```

这会移除该应用的下载隔离属性。输入密码时终端不会显示字符，完成后重新打开应用。

### 检查更新

在 **设置 → 软件更新**中手动检查，或选择启动时检查。新版本会按系统匹配安装包，按界面提示继续即可。Android 与桌面端独立更新；更新检查失败不会影响正常阅读。

---

## 常见问题

| 遇到的情况 | 可以先这样处理 |
| --- | --- |
| AI 按钮不可用 | 确认 AI 阅读已启用，默认服务和模型可用 |
| 连接测试成功，生成仍失败 | 检查模型名称及请求参数支持情况，长文章也可能超出服务限制 |
| Citation 无法定位 | 等待正文加载；文章变化或被删除时，改看来源信息 |
| 全文提取不完整 | 重试提取，或用原文查看评论、图表、登录后内容 |
| RSSHub 已匹配却无内容 | 测试并切换实例，查看具体错误 |
| 网站返回 403 / 418 | 检查网址和网络，稍后重试；网站可能限制频率、地区或自动请求 |
| 分享缺少译文或摘要 | 先在当前阅读页打开对应内容，再右键分享按钮确认选择 |
| 加密备份恢复失败 | 先在备份页面输入原密码，再选择恢复文件 |

需要帮助或提出建议，请[提交 Issue](https://github.com/ZGMFX01A/OrigRead-Desktop/issues)。项目目前不接受 Pull Request，翻译和文档纠错也请通过 Issue 反馈。

---

## 其他平台

[OrigRead Android](https://github.com/ZGMFX01A/OrigRead) 适用于 Android 手机和平板。

[桌面端仓库](https://github.com/ZGMFX01A/OrigRead-Desktop) · [下载更新](https://github.com/ZGMFX01A/OrigRead-Desktop/releases/latest) · [问题反馈](https://github.com/ZGMFX01A/OrigRead-Desktop/issues)
