# 原读 Desktop 操作手册

语言：简体中文。英文版请查看项目仓库中的 `USER_GUIDE.md`。

## 快速索引

- [快速开始](#快速开始)
- [添加一个来源](#添加一个来源)
- [阅读文章](#阅读文章)
- [使用 AI 摘要](#使用-ai-摘要)
- [翻译文章](#翻译文章)
- [朗读文章](#朗读文章)
- [键盘快捷键](#键盘快捷键)
- [添加来源失败怎么办](#添加来源失败怎么办)
- [管理 RSSHub](#管理-rsshub)
- [使用账户与同步](#使用账户与同步)
- [规则与过滤](#规则与过滤)
- [OPML、备份与迁移](#opml备份与迁移)
- [常见问题](#常见问题)

---

## 快速开始

第一次打开原读 Desktop，可以按下面顺序完成最基本的阅读流程：

1. 打开应用，保持默认 **Local** 账户即可。
2. 选择 **+ 添加**，粘贴一个 RSS/Feed 或网站地址。
3. 等待来源检测完成，选择推荐候选并添加。
4. 回到文章列表，打开一篇文章。
5. 如果来源只给摘要，切换到全文；需要查看真实网页时打开原文。
6. 需要 AI 摘要或翻译时，再到设置里配置对应服务。

不需要一开始就配置 FreshRSS、RSSHub 自建实例、Website Rule 或 AI。普通 RSS/网站可以先直接添加，遇到确实需要的情况再使用高级功能。

---

## 添加一个来源

### 添加网站、RSS 或 Feed

1. 在左侧区域选择 **+ 添加**，进入添加来源。
2. 粘贴网站首页、文章列表页、RSS/Atom Feed 或其他来源地址。
3. 开始检测后等待分析完成。窗口会显示当前正在检查的阶段和已等待时间。
4. 查看候选结果。原读会推荐一个更适合订阅的候选；如果有多个可用结果，也可以自己选择。
5. 确认添加，返回时间线等待首次同步。

### 候选类型怎么选

| 候选 | 适合什么情况 | 建议 |
| --- | --- | --- |
| **RSS / Atom** | 网站本身提供标准 Feed | 通常优先，稳定且刷新快 |
| **RSSHub** | RSSHub 已有对应路由 | 路由可用时很方便；一个网站可能有多个频道 |
| **JSON/API** | 网站有稳定公开接口 | 一般不容易受网页视觉改版影响 |
| **Website** | 没有 Feed，但网页有稳定文章列表 | 可以使用；网站改版后可能需要换规则 |
| **动态网页** | 必须执行 JavaScript 才能看到文章列表 | 最后兜底，检测和刷新通常更慢 |

如果多个候选都正常，优先看**后续能不能稳定刷新、标题和链接是否正确**，不需要纠结哪种方式“技术上更高级”。

### 从 OPML 导入

如果已经在其他 RSS 阅读器里有订阅：

1. 在原阅读器中导出 `.opml` / `.xml`。
2. 在原读 Desktop 的添加菜单中选择 **导入 OPML**。
3. 选择文件并确认。
4. 导入后检查分组和来源数量。

OPML 只负责标准 Feed 迁移。原读自己的规则、RSSHub、AI/翻译设置等请使用“完整配置备份”。

---

## 阅读文章

### 来源正文、全文和原文有什么区别

- **来源正文**：RSS/Atom/JSON 自己提供的内容。有些来源只提供摘要。
- **全文**：原读访问文章页面后提取出的正文，适合阅读、搜索、翻译和 AI 摘要。
- **原文**：网站真实页面，在应用内网页视图中打开。

如果当前文章只有几行摘要，先切换全文；如果全文提取仍不完整，直接打开原文即可。

### 标记文章

阅读时可以直接：

- 已读 / 未读；
- 收藏 / 取消收藏；
- 上一篇 / 下一篇；
- 搜索当前正文；
- 打开原文。

### 调整阅读外观

在设置中可以调整：

- 阅读字体；
- 导入本地字体；
- 字号；
- 阅读背景色；
- 浅色 / 深色 / 跟随系统主题。

这些外观设置只改变阅读体验，不会修改文章内容。

---

## 使用 AI 摘要

### 第一次使用前配置 AI 服务

1. 打开 **设置 → AI 阅读**。
2. 添加一个 OpenAI Compatible 服务。
3. 填写 Endpoint；需要鉴权时填写 API Key。
4. 获取或填写模型，并设置默认模型。
5. 选择 **测试连接**。
6. 测试成功后保存并启用。

可以配置多个服务，每个服务维护自己的地址、密钥和模型。

### 生成摘要

1. 打开一篇文章。
2. 选择 **AI 摘要**。
3. 原读按当前默认服务、模型和摘要档位开始生成。
4. 生成期间会显示“准备文章 / 等待 AI / 整理并保存”等实际阶段和已等待时间。
5. 不需要继续时，可以停止当前请求。

### 临时换服务、模型或摘要档位

在摘要选项中可以临时选择：

- Provider；
- Model；
- 速览 / 均衡 / 深入。

临时选择只影响本次生成，不需要修改全局默认设置。

### 调整摘要位置

AI 摘要可以：

- 替换正文；
- 停靠在左侧；
- 停靠在右侧；
- 停靠在上方；
- 停靠在下方。

停靠时可以调整面板大小；也可以直接使用快捷键切换位置和尺寸。

### 使用 AI 生成规则

桌面端支持在规则设置页生成 Website Rule 和 JSON/API Rule。它的作用是根据一个真实可访问的列表页或接口，先生成一个候选规则，再让本地解析器试跑确认；它不是把任意网站变成稳定订阅的保证。

1. 打开 **设置 → 网站解析规则** 或 **设置 → JSON 规则**，点击 **AI 生成**。
2. 填写目标地址。Website Rule 填文章列表页；JSON/API Rule 填公开 JSON 接口，或包含 Next.js / Nuxt 静态内嵌 JSON 的页面。
3. 页面会带入 AI 设置里的默认服务和模型。需要时可以在生成前选择其他已启用的服务或模型。
4. 等待界面依次完成抓取、分析、生成候选和本地校验。候选结果会显示文章数量、评分和样例标题。
5. 确认标题、链接和文章数量确实正确后，再点击 **保存规则**。没有通过本地校验的候选不会自动保存。

如果页面依赖 JavaScript、要求登录、验证码或付费权限，普通抓取可能无法得到可用样本。应用不会绕过这些访问限制；这时可以改用来源发现里的动态网页方式、公开 API 或手动规则。桌面端 AI 生成只负责先验证文章列表，正文打开仍使用常规正文提取和内置原文查看兜底。

### 如何判断生成结果

- **本地校验通过**：列表规则已经用目标样本真实跑通，可以保存候选。
- **文章数量或样例不对**：不要保存，返回修改目标地址或换模型重新生成。
- **生成失败**：先看界面显示的失败阶段和原因。常见原因是地址不是公开 JSON、页面被拦截，或模型返回的选择器没有解析出文章。

---

## 翻译文章

### 配置翻译服务

1. 打开 **设置 → 翻译**。
2. 启用需要的翻译方式。
3. 配置对应服务需要的地址、Key 或其他参数。
4. 设置默认目标语言和默认服务。
5. 使用连接测试确认服务可用。

Desktop 可以使用 Microsoft Translator、DeepL、Google Cloud Translation、DeepLX/DLX，也可以使用 OpenAI Compatible 模型做全文翻译。

### 翻译当前文章

1. 打开文章。
2. 选择翻译入口。
3. 使用当前默认服务和目标语言开始翻译。
4. 翻译完成后，根据需要查看译文或原文/译文内容。

传统翻译和 AI 摘要互不依赖。没有配置 AI 摘要，也可以正常使用 DeepL、Microsoft 等翻译服务。

---

## 朗读文章

原读 Desktop 把朗读对象分开处理：

- 正文朗读；
- 译文朗读；
- AI 摘要朗读。

正在阅读译文时，主朗读入口优先朗读译文；AI 摘要面板有自己的朗读入口。

系统提供哪些声音取决于当前操作系统和 Electron 能读取到的语音列表。

---

## 键盘快捷键

阅读时可以使用：

| 快捷键 | 功能 |
| --- | --- |
| `↑` / `↓` | 正文向上 / 向下滚动 |
| `←` / `→` | 上一篇 / 下一篇 |
| `K` / `J` | 上一篇 / 下一篇（兼容快捷键） |
| `M` | 已读 / 未读 |
| `S` | 收藏 / 取消收藏 |
| `U` | 打开 / 关闭原文 |
| `[` | 收起 / 展开工作区 |
| `Ctrl/Cmd + F` | 搜索当前正文 |
| `,` / `.` | 循环切换 AI 摘要位置 |
| `-` / `+` | 缩小 / 放大停靠的 AI 摘要区域 |

当光标正在输入框、弹窗或设置控件中时，阅读快捷键会让位给当前输入操作。

---

## 添加来源失败怎么办

### 先看当前检测阶段

添加来源窗口会告诉你当前正在检查 RSS、RSSHub、JSON/API、静态 Website 还是动态页面。

如果某个阶段较慢，先等待它结束。尤其是公共 RSSHub 实例和动态网页，本来就可能比直接 RSS 慢。

### “动态 Chromium 兜底”是什么意思

有些网站初始 HTML 里没有文章列表，必须执行 JavaScript 后才出现内容。普通解析拿不到文章时，Desktop 会在最后自动启动受限 Chromium 重新读取页面。

你不需要手动开启这个过程。

如果动态页面确实解析到文章，但结果稳定性一般，候选可能带有提示。建议：

1. 先看标题和文章数量是否正常；
2. 内容合理时可以尝试添加；
3. 添加后观察后续刷新是否持续正常；
4. 经常失败时优先换成 RSS、RSSHub、API 或明确规则。

**网页能显示出来不等于可以订阅。** 如果最终没有识别到有效文章链接，原读不会创建一个空来源。

### 浏览器能打开，原读却不能订阅

常见原因：

- 需要登录；
- CAPTCHA / 浏览器挑战；
- 需要复杂交互后才加载列表；
- 页面没有稳定文章列表；
- 网站限制自动请求；
- 当前网络无法访问 RSSHub/API。

原读不会绕过登录、验证码、付费墙和网站访问控制。此时优先找官方 Feed、RSSHub 路由或公开 API。

---

## 管理 RSSHub

打开 **设置 → RSSHub**，可以：

1. 启用 / 停用 RSSHub；
2. 启用或禁用实例；
3. 添加公共或自建实例；
4. 单独测试实例；
5. 恢复默认实例设置。

### “已匹配”为什么还不能订阅

原读先在本地匹配 RSSHub 路由，再访问实例。因此：

**路由已匹配 ≠ 当前实例一定可用。**

常见状态：

- **可订阅**：实例成功生成 Feed，并且内容可用；
- **实例超时 / 不可访问**：路由存在，但本次实例请求失败；
- **未返回有效 Feed**：实例响应了，但结果不是可用 Feed；
- **内容未通过质量检查**：拿到了内容，但不适合直接作为时间线来源；
- **需要更具体的 URL**：当前地址缺少该路由需要的参数。

公共实例有波动时，可以稍后重试或切换实例。

---

## 使用账户与同步

### Local

普通用户默认使用 Local 即可。Local 可以使用原读全部来源类型：

- RSS / Atom；
- RSSHub；
- Website；
- JSON/API。

### FreshRSS / Google Reader Compatible

如果你有对应的服务器，可以添加远端账户来同步订阅、分组、文章、已读和收藏状态。

新增/移动/重命名 RSS 来源时，操作会按照远端服务协议执行，而不是偷偷只保存到本机。

### Fever Compatible

Fever 账户同步协议本身提供的 Feed、文章、未读和收藏能力。协议没有完整订阅维护能力时，原读也不会伪装成可以远端新增、移动或重命名 Feed。

### 同步设置

同步间隔、启动同步等设置属于当前账户。不同账户可以使用自己的同步配置。

如果你不需要服务器同步，保持 Local 就够了，不必为了“功能更多”建立远端账户。

---

## 规则与过滤

### Website Rule

网站没有可用 Feed，但 HTML 文章列表比较稳定时使用。

### JSON/API Rule

网站有稳定 REST/JSON 或其他结构化接口时使用。能用稳定 API 时，一般比依赖页面 CSS 结构更耐改版。

JSON/API Rule 只负责从列表数据中找到文章标题、链接和其他已存在字段；它不会凭空创造正文，也不会绕过登录、签名、验证码或付费墙。没有可复用的正文字段时，打开文章仍会使用普通正文提取和内置原文查看。

### 文章过滤

用于“这个来源我要订阅，但某些标题永远不想进时间线”。支持关键词和正则。

新建过滤规则只影响之后抓到的新文章，不会反向删除历史文章。

普通用户不需要为了添加常见网站先学会写规则。先使用自动来源发现，只有自动方式确实不够时再考虑规则。

---

## OPML、备份与迁移

### OPML 适合什么

OPML 用于和其他 RSS 阅读器交换标准订阅。

### 完整配置备份适合什么

如果要在 OrigRead Android / Desktop 之间迁移更多内容，使用完整配置备份。它可以包含：

- 当前账户订阅和分组；
- Website / JSON 规则；
- 过滤规则；
- RSSHub 配置；
- 阅读偏好；
- 翻译和 AI 配置。

文章正文、已读/收藏历史、AI 摘要缓存、翻译缓存等运行数据不属于完整配置备份。

敏感凭据默认不导出。只有主动选择包含凭据并设置备份密码时，才会生成可迁移的加密凭据块。

---

## 软件更新

在 **设置 → 软件更新** 中可以手动检查新版本，也可以选择启动时自动检查。

有新版本时，原读会根据当前系统选择对应安装包。网络失败或 GitHub 暂时不可访问不会影响正常阅读。

---

## 常见问题

### 添加来源一直停在 RSSHub

公共 RSSHub 实例可能响应较慢或暂时不可用。等待本轮检测完成；如果经常发生，可以在 **设置 → RSSHub** 中禁用不稳定实例或使用自己的实例。

### RSSHub 已匹配但没有可订阅项

说明本地路由已经找到，但实例请求或最终 Feed 没通过。查看具体状态，不需要重复删除/添加同一个网址。

### AI 连接测试成功，但生成仍失败

连接成功只证明 Endpoint/Key 基本可访问。继续检查当前模型是否真的支持请求参数，以及模型名称是否正确。

### DeepL 测试和额度查询有什么区别

连接测试只验证翻译请求；额度查询是单独操作。不要把“额度接口失败”理解成“翻译接口一定不能用”。

### 原文网页和正文显示不一致

正文是提取后的阅读版本，原文是网站真实页面，两者本来就可能不同。遇到复杂表格、评论、交互组件或登录内容时，以原文为准。

### 动态来源刷新比较慢

动态来源要启动 Chromium、等待页面脚本，再解析渲染后的页面，因此比标准 RSS 慢。能使用 RSS/RSSHub/API 时优先使用这些方式通常更稳定。

---

## 其他平台

📱 **OrigRead Android**：https://github.com/ZGMFX01A/OrigRead
